<UserControl x:Class="Grombcross.Views.PuzzleGameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:viewmodels="clr-namespace:Grombcross.ViewModels" xmlns:converters="clr-namespace:Grombcross.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="800">
    <!--<UserControl.DataContext>
        <viewmodels:PuzzleGameViewModel/>
    </UserControl.DataContext>-->

    <UserControl.Resources>
        <DataTemplate x:Key="DataTemplate_Level2">
            <Button Margin="5" Width="50" Height="50" Style="{Binding State, Converter={StaticResource BlockToButtonStyleConverter}}" Click="LeftClickBlock">
                <!--<Button.InputBindings>
                    <MouseBinding Gesture="LeftClick" Command="LeftClickBlock" />
                    <MouseBinding Gesture="RightClick" Command="RightClickBlock" />
                </Button.InputBindings>-->
            </Button>
        </DataTemplate>

        <DataTemplate x:Key="DataTemplate_Level1">
            <ItemsControl ItemsSource="{Binding}" ItemTemplate="{DynamicResource DataTemplate_Level2}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <ItemsControl x:Name="PicrossGrid" ItemsSource="{Binding Blocks}" ItemTemplate="{DynamicResource DataTemplate_Level1}" HorizontalAlignment="Center" VerticalAlignment="Center">
            <ItemsControl.RenderTransform>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </ItemsControl.RenderTransform>
            <ItemsControl.RenderTransformOrigin>
                <Point X="0.5" Y="0.5"/>
            </ItemsControl.RenderTransformOrigin>
        </ItemsControl>
    </Grid>
</UserControl>
